import SectionTitle from "../../../components/SectionTitle/SectionTitle";

const ReviewSection = () => {
    return (
        <div>
            <SectionTitle
            heading="Student Review Section"
            ></SectionTitle>
        </div>
    );
};

export default ReviewSection;